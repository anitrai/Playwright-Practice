// const {test} = require("@playwright/test");

// test('first test cases',async function({browser}){

 

// })

// multiples of 2 & 5

for(let i = 1;i<=10;i++){
    if(i%2 == 0 && i%5 == 0)
        console.log("Multiples of 2 & 5:", i);
}